<h1>Let's create a movie list</h1>
<%= simple_form_for @list do |f| %>
  <%= f.input :comment %>
  <%= f.association :category, as: :check_boxes %>
  <%= f.submit "Submit name", class: "btn btn-primary" %>
<% end %>
